<div class="row">
  <aside class="col-md-4">
    <h3>マイページ</h3>      
    <section class="user_info">
    <h5>
    <%= @user.name %>
    </h5>
    <hr>
    </section>
  </aside>
  <div class="row">
  <div class="col-md-8">  
    <%= image_tag @map.image %>
    <br>
    <!--Hint btn-->
    <%= f.submit "Get Hint", class: "btn btn-sm btn-warning" %>
    <!--Reward btn-->
    <% if @map.status1 == true && @map.status2 == true && @map.status3 == true && @map.status4 == true%>  
    <%= f.submit "Get Reward", class: "btn btn-sm btn-warning active" %>
    <% else %>
    <%= f.submit "Get Reward", class="btn btn-warning disabled" %>
    <% end %>
  
    <!--<div class="col-md-8"><a><img src="/images/map.png"/></a> -->
    <% if @map.status1 == true %>
     <img class=imgA1 src="/images/stamp1.png">
    <% else %>
     <img class=imgA1 src="/images/blank.jpeg">
    <% end %>
    <% if @map.status2 == true %>
     <img class=imgB1 src="/images/stamp2.png">
    <% else %>
     <img class=imgB1 src="/images/blank.jpeg">
    <% end %>
    <% if @map.status3 == true %>
     <img class=imgC1 src="/images/stamp3.png">
    <% else %>
     <img class=imgC1 src="/images/blank.jpeg">
    <% end %>
    <% if @map.status4 == true %>
     <img class=imgD1 src="/images/stamp4.png">
    <% else %>
     <img class=imgD1 src="/images/blank.jpeg">
    <% end %>
     
  </div>

  <div class="col-md-4">
  <%= form_for(@user, :url => {:action => 'update'}) do |f| %>
  <%= render 'shared/error_messages', object: f.object %> <!-- エラー表示 -->
    <label for="stampcode" class="control-label col-md-1">CODE:</label>
    <input type="text" name="code", placeholder="popy_0000"></input>
    <%= f.submit "Confirm", class: "btn btn-sm btn-warning" %>
  <% end %>
  <br><br>

  <% @stamp_all.each do |f| %>
    <%= link_to image_tag f.image, stamp_update_user_path(:stamp_image => f.image) %>
    
  <% end %>
  
  </div>
  </div>
</div>

    <% link_to image_tag("/images/stamp1.png", :border => 0, :alt => '', :title => ''), login_path%>
    <% link_to image_tag("/images/stamp2.png", :border => 0, :alt => '', :title => ''), login_path%>
    <% link_to image_tag("/images/stamp3.png", :border => 0, :alt => '', :title => ''), login_path%>
    <% link_to image_tag("/images/stamp4.png", :border => 0, :alt => '', :title => ''), login_path%>
